---
anta.tests.routing:
  generic:
    - VerifyRoutingTableEntry:
        vrf: default
        routes:
          - 10.1.0.1
        collect: all
    - VerifyRoutingTableEntry:
        vrf: default
        routes:
          - 10.1.0.2
        collect: all
    - VerifyRoutingTableEntry:
        vrf: default
        routes:
          - 10.1.0.3
        collect: all
    - VerifyRoutingTableEntry:
        vrf: default
        routes:
          - 10.1.0.4
        collect: all
    - VerifyRoutingTableEntry:
        vrf: default
        routes:
          - 10.1.0.5
        collect: all
