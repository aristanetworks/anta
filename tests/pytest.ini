[pytest]
render_collapsed = true
log_cli = true
filterwarnings =
    ignore::urllib3.exceptions.InsecureRequestWarning
markers =
    # Generic tags
    debug: to run only debug test (deselect with '-m "not debug"')
addopts = --ignore=tests/scripts
